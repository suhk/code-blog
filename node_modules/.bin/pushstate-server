#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pushstate-server/bin/pushstate-server" "$@"
  ret=$?
else 
  node  "$basedir/../pushstate-server/bin/pushstate-server" "$@"
  ret=$?
fi
exit $ret
